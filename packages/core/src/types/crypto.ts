/**
 * Type definitions related to cryptography.
 */

/**
 * Represents a privacy-preserving pepper.
 * Define as a string or byte array for now.
 */
export type Pepper = string; // Or Uint8Array if you prefer byte representation
